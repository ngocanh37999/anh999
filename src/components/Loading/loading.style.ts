import { LinearProgress } from '@material-ui/core'
import styled from 'styled-components'
export const StyledLoading = styled(LinearProgress)`
  position: fixed !important;
  top: 0;
  left: 0;
  right: 0;
  z-index: 1000001;
`
