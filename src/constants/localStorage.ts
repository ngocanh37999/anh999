interface TypeLocalStorage {
  user: string
  accessToken: string
  cart: string
  product: string
}

const LocalStorage: TypeLocalStorage = {
  user: 'user',
  accessToken: 'accessToken',
  cart: 'cart',
  product: 'product'
}
export default LocalStorage
